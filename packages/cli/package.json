{
  "name": "@puddle-code/cli",
  "version": "0.0.1",
  "description": "puddle — self-hosted orchestrator for CLI coding agents: serves the cockpit UI and connects it to local or SSH hosts.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "puddle": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "@puddle/daemon": "workspace:*",
    "@puddle/shared": "workspace:*",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.18.0",
    "esbuild": "^0.25.0",
    "ws": "^8.21.0"
  }
}
